function calculate(x, y) {
    const sum = x + y;
    const product = x * y;
    return sum + product;
}

const a = 10;
const b = 20;
const result = calculate(a, b);
console.log(result);
console.log(a + b);